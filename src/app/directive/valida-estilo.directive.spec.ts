import { ValidaEstiloDirective } from './valida-estilo.directive';

describe('ValidaEstiloDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaEstiloDirective();
    expect(directive).toBeTruthy();
  });
});
